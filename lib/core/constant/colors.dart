import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF000000); // black
  static const accent = Color.fromARGB(255, 0, 255, 128); // cyan
  static const backgroundDark = Color(0xFF121212); // dark background
  static const card = Color(0xFF1E1E1E); // dark card
  static const textDark = Colors.white;
  static const greyLight = Color(0xFF2C2C2C);
}
